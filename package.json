{
  "name": "rhw-research-db",
  "version": "0.1.0",
  "description": "RHW CPAs Research & Knowledge Base - Firm-wide research database with SOPs, policies, and Q&A transcripts",
  "private": true,
  "workspaces": [
    "frontend",
    "api"
  ],
  "scripts": {
    "install-all": "npm install && npm install --workspace=frontend && npm install --workspace=api",
    "dev": "npm run dev --workspace=frontend & npm run dev --workspace=api",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:api": "npm run dev --workspace=api",
    "build": "npm run build --workspace=frontend && npm run build --workspace=api",
    "build:frontend": "npm run build --workspace=frontend",
    "build:api": "npm run build --workspace=api",
    "test": "npm run test --workspace=frontend && npm run test --workspace=api",
    "test:frontend": "npm run test --workspace=frontend",
    "test:api": "npm run test --workspace=api",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=api",
    "type-check": "npm run type-check --workspace=frontend && npm run type-check --workspace=api"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "research",
    "knowledge-base",
    "cpa",
    "rhw",
    "sop",
    "policy"
  ]
}
